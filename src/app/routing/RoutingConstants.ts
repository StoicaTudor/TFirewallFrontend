export enum Paths {
  Undefined = "**",
  Home = '',
  Logs = 'logs',
  SignIn = 'sign-in',
  SignUp = 'sign-up',
  Logout = 'logout',
  DefaultPath = '/',
}

export enum PathMatch {
  Prefix = "**",
  Full = "full",
}
